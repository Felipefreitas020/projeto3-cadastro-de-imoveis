let moveis = []
let option = true

while (option !== '3') {
    let menu = ('  Cadastro de Imóveis : ') + deck.length + '\n\n'
    menu += ('1.adicionar imovel:\n')
    menu += ('2 - listar imovel\n')
    menu += ('2 - Puxar uma carta\n')
    menu += ( '3 - sair\n')
    
}
